# 새 창 첫마디 (이 파일 전체를 복사해 새 창에 붙여넣으십시오)

【현장비서 도구모음】 인수인계 읽고 이어서 해 — 2026-09-15 작성

## 지금까지 된 것
- `_현장비서\도구모음.bat` 으로 도는 **파이썬 도구 23종**을 만들었다 (토큰 0, 로컬).
  파일은 `현장비서_도구_260915.zip`. GitHub 백업: gamakatu-PM/mic-pm 브랜치 `claude/cowork-suggestions-bohllt`
- 도구 목록: 아침브리핑/콜카드/PLAUD미처리 · 회의록검색/전화번호사전 · 객실수량표/CB외함/견적서채우기/증감비교 ·
  제안서초안/자재사양서/시방서/부서메일/사진대지 · 납기역산/수금레이더/월말집계 ·
  서식검사/단가장검진/의뢰서대장/파일정리/신규현장레이더/자가진단
- 기존 `시작.bat`(PLAUD 회의록, km-19·km-30)은 **한 줄도 건드리지 않았다.**

## 절대 뒤집지 말 것 (이번 창 확정)
1. 도구는 파일마다 쪼갠다. 하나가 고장나도 나머지가 돈다.
2. 금액·요율·배수는 도구가 정하지 않는다. 빈칸으로 두고 프로님이 넣는다.
3. 도구는 파일을 지우거나 옮기지 않는다. 파일정리도 계획만 낸다.
4. 원틀은 새로 그리지 않는다. `_원틀` 복사 후 값만 치환한다.
5. 제안서는 하루 한 장을 억지로 만들지 않는다. 자료 요청이 나온 날만.
6. 26년 회의록 구글시트 방식은 2026-09-12부로 중지. 로컬 `_현장비서`가 정본.
7. 결과는 `_도구결과\{도구}\{YYMMDD}\` 에만 떨어진다.

## 아직 못 한 것 (새 창이 이어갈 것)
- `1_결정대기_v1_260915.xlsx` 18건이 답변 대기 중. **★급함 5건부터.**
- 원틀 4종(견적서·작업의뢰서·자재사양서·시방서·제안서)을 못 받아 그 도구들은 껍데기만 검증됨.
- 사진대지는 렌더 육안 확인을 못 했다 (작업방 LibreOffice 고장).
- 건축HUB API 키 없음 → 신규현장 레이더 미검증.

## 새 창이 할 일
1. `1_결정대기` 엑셀의 답변칸을 먼저 읽는다. 답이 채워져 있으면 그것부터 처리한다.
2. 새로 여쭐 것이 생기면 **그 엑셀에 줄을 추가해 새 버전으로 낸다** (v2, v3...). 이전 판은 `버전\` 으로.
3. 스킬 `km-31-hyeonjang-tools` 가 저장돼 있는지 확인한다. 없으면 `3_스킬_저장대기\` 의 파일을 다시 보낸다.
