# KM 손바닥 — 고치는 법 (새 창이 먼저 읽는다)

프로님 : *"난 코드 못 고쳐. **앱을 수정하는 것이 나중에 쉽게** 하라는 뜻이야."*
→ 그래서 화면별로 파일을 갈랐다. **고칠 곳만 열고, 시험 한 번 돌리고, 그 파일만 다시 올린다.**

## 어디를 고치면 되나
| 고쳐 달라는 말 | 여는 파일 |
|---|---|
| 「여정 화면이 …」 「현장 카드가 …」 「진행률이 …」 | `30-map.js` |
| 「단계 / 열쇠 문장 / 게이트 / 다음 한 수」 | `20-quests.js` |
| 「오늘 화면 / 답하기 단추 / 연락 끊긴 곳」 | `40-today.js` |
| 「창고 / 질문 / 정해진 것 찾기」 | `45-ask.js` |
| 「현장 상세 / 공정·사람·돈·경계 탭」 | `50-sites.js` |
| 「회의 화면 / 초안 5종」 | `60-meet.js` |
| 「설정 / PC 붙여넣기 / 아침 한 장 가져오기」 | `70-settings.js` |
| 「앱 내용 고치기 화면」 | `71-edit.js` |
| 「PC·대장에서 들어오는 것 / 제가 채운 것」 | `15-feed.js` |
| 「보내기 묶음」 | `13-outbox.js` |
| 「저장·공유 저장소·동기화」 | `10-core.js` |
| 「시트·문안창·마이크」 | `11-ui.js` |
| 「탭 이동」 | `12-router.js` |
| 「색·글자·간격」 | `km.css` |
| 「탭을 하나 더」 「화면 뼈대」 | `index.html` + `12-router.js` |

## 고치는 순서 (이것만 지키면 안 깨진다)
1. **그 파일만** 연다. 다른 파일은 열지 않는다.
2. 고친다.
3. `node 앱/KM손바닥_자가시험.mjs` 를 돌린다 → **14개 전부 통과**여야 한다.
4. 통과하면 그 파일 하나만 다시 올린다 (Artifact publish, `url` 은 항상 같은 주소, `files` 에 바꾼 파일만).
5. 실패하면 **고친 것을 되돌리고** 원인을 찾는다. 통과 못 한 판은 올리지 않는다.

## 파일이 갈려서 생기는 함정 (겪은 것)
- 다른 파일의 함수를 **즉시** 참조하면 깨진다. `onclick=sendOutbox` → `onclick=()=>sendOutbox()` 로 미룬다.
- 최상위 `let/const` 는 **번호 순서**대로 만들어진다. `20-quests.js` 의 `QUESTS` 를 `10-core.js` 에서 바로 쓰면 없다. 함수 안에서 쓰는 것은 괜찮다.
- 그래서 파일 이름의 번호가 곧 실행 순서다. 새 파일을 넣을 때 번호를 잘 고른다.

## 주소와 저장소
- 앱 주소는 **바뀌지 않는다** : https://claude.ai/artifact/Mg52NGzLvSegFJYXhuzHwH
- 자료(현장·할 일·질문·설정)는 앱이 아니라 공유 저장소에 있다. 코드를 다시 올려도 자료는 그대로다.
- 열쇠 문장·품목 목록은 코드가 아니라 `config/quests` 에 있다. 그건 파일을 고치지 말고 저장소를 고친다(v6.8).

## 되돌리기
- 코드 : `앱/KM손바닥_v6.x.html` 과 `앱/v6.9_화면별파일/` 이 판별로 남아 있다.
- 내용 : `config/bak_001`, `bak_002` … 「3번 되돌려」 한마디면 그 판으로.
