# KM 현장비서 도구모음 (토큰 0)

배성윤 프로 데스크탑에서 **클로드 호출 없이** 도는 파이썬 도구 25종.
기존 `_현장비서\시작.bat`(PLAUD 회의록)은 건드리지 않고 옆에 붙는 별도 런처다.

| # | 도구 | 파일 | 입력 | 산출 |
|---|---|---|---|---|
| 1 | 회의록 검색기 | t01_search.py | 찾을 말 | 현장/파일/줄 CSV |
| 2 | 전화번호 사전 | t02_phonebook.py | 번호·이름 | 사전 CSV + 즉시 조회 |
| 3 | 객실 수량표 | t03_roomqty.py | 성급·실수 | 기구물 물량 CSV |
| 4 | CB 외함 세트 | t04_enclosure.py | W×H×D | 속판·커버·부스바 |
| 5 | 납기 역산 | t05_schedule.py | 준공일 | 단계별 기한 + D-day |
| 6 | 수금 레이더 | t06_collect.py | 수금대장.csv | 미발행/입금대기 |
| 7 | 증감(Rev) 비교 | t07_delta.py | 수량표 2개 | 증·감 목록 |
| 8 | 사진대지 | t08_photo.py | 사진 폴더 | A4 엑셀 |
| 9 | 엑셀 서식 검사 | t09_fmtcheck.py | xlsx | 위반 칸 목록 |
| 10 | 의뢰서 발행대장 | t10_orderlog.py | (자동) | 발행 목록 |
| 11 | PLAUD 미처리 감지 | t11_plaudgap.py | (자동) | 누락 경보 |
| 12 | 파일 정리 계획 | t12_fileorg.py | 폴더 | 이동 계획(실행 안 함) |

## 지키는 규칙
- 금액·요율·배수는 정하지 않는다. 빈칸으로 두고 프로님이 넣는다.
- 파일을 지우거나 옮기지 않는다. 12번도 계획만 낸다.
- 윈도우 금지문자는 `safe_name()` 으로 거른다. bat/txt 는 cp949.
- 결과는 `_도구결과\{도구}\{YYMMDD}\` 에만 떨어진다.

## 설치
`_처음_한번만_설치.bat` → `도구모음.bat`


## v2 추가 (11종)
| 도구 | 파일 | 비고 |
|---|---|---|
| 아침 브리핑 | t13_brief.py | 납기+수금+미처리를 HTML 한 장, 신호등 3색 |
| 오늘의 콜 카드 | t14_callcard.py | 마지막 통화일 기준 정렬 |
| 제안서 후보+초안 | t15_proposal.py | 자료 요청이 나온 건만. 하루 한 장을 억지로 채우지 않음 |
| 부서 전달 메일 | t16_deptmail.py | 회의록 2부 → 부서별 초안 txt. 발송 안 함 |
| 단가장 검진 | t17_pricecheck.py | 요율 하드코딩·검산 0·동명이인·노란칸 임시단가 |
| 견적서 채우기 | t18_quotefill.py | 원틀 복사 후 품명/수량만. 단가는 비움 |
| 자재사양서 | t19_matspec.py | pptx 원틀 zip 치환 (서식 불변) |
| 시방서 치환 | t20_specbook.py | hwpx/docx 원틀 치환 |
| 월말 계산서 집계 | t21_taxmonth.py | 월별 발행/입금/미수 |
| 신규 현장 레이더 | t22_siteradar.py | 건축HUB API. 키는 설정.ini [레이더] |
| 자가진단 | t23_selfcheck.py | 경로·부품·원틀·대장·도구 23개 점검 |

공용 : `common.py`(경로·safe_name·CSV·HTML·원틀찾기·zip치환·로그), `sitebook.py`(현장대장)

## 도구를 파일마다 쪼갠 이유
하나가 고장나도 나머지가 돈다. 고칠 때도 그 파일만 보면 된다.
메뉴(`menu.py`)는 목록만 들고 있고, 실행 순간에 그 모듈만 불러온다.

| 인수인계함 | t24_handover.py | 결정대기 엑셀의 미답변 건을 띄우고 폴더를 연다 |
| 이미지 모음 PPT | t25_imagedeck.py | 참고사진 → 16:9 PPT + 출처대장 CSV. 출처 미기재는 빨간 경고 |

## v33 추가
| 도구 | 파일 | 비고 |
|---|---|---|
| 회의 연결 | t40_meeting.py | 받는함 txt → _현장비서 대기함 + 시작.bat / 회의록 「수량·규격 변경」 → 부탁서·현황판 ⑦ / 회의록 코드 백업 zip. 36번이 자동 호출 |
| 총괄 점검 | t41_totalcheck.py | 판·폴더·회의록·도면→돈(37)·39·38 을 한 표로. 36번 끝에 빠른 점검으로 `_총괄점검.md` 갱신 |
| 아침 한 장 | t42_morning.py | 7층(할 것·현장 카드·부서 의뢰서·회의 이력·돈·점검·표). 업무판 csv+회의록 원문+도면 결과+수금대장+확정 대장. 36 끝·35 메일 |
| 확정 입력·찾기 | t43_confirm.py | 프로님이 고친 값 = 결정 사항 (`facts.py` 확정사항.csv, 추정보다 우선, 이력 보존, ?찾기) |

## v35
도면 폴더에 연도 폴더(`26년`·`2026`)를 두어도 현장을 찾는다 (`t31_intake.year_dirs/sites(with_year)`). 연도 폴더 자체는 현장으로 잡지 않고, 새 도면은 최신 연도 폴더로, 같은 현장이 여러 연도에 있으면 최신 연도만 읽는다.

## v36
`도면\{현장명}\26년\`(현장 안 연도 폴더)도 읽는다 — **최신 연도만**, 옛 연도는 보관용으로 건너뛴다(`site_year_dirs/_old_year/skipped_years`). 41 총괄 점검 B 구역에 도면 폴더 구조·현장별 읽는 위치·건너뛴 연도가 찍힌다.
| 회의록 확인 | t44_meetingcheck.py | 저장만 해 둔 PLAUD 회의록을 읽었다고 표시(`facts.checked/check_meeting`, 회의확인.csv). 미확인은 42 아침 한 장 **0층**·41 C 구역에 매일 |

## v38
미확인 회의록을 **대시보드(0층 펼치기) + 엑셀 7시트 + 아침 메일 첨부**로 낸다 (`t44.build_xlsx`, `t35.send(files=)`). 클로드용 md 에도 안건·결정·조치·할 일·변경·대외금지가 줄로 들어간다. `t42.section()` 이 머리글 줄 남은 글자를 내용으로 잡던 것 수정.

## v39
**45 요청 분기 · 보낼 메일** (`t45_askgate.py`) — 「견적 주세요」 「제안서 주세요」 가 왔을 때 도구가 도면 유무로 갈라 준다.

| 경우 | 도구가 하는 것 |
|---|---|
| 도면 있음 | 「견적 만들 수 있음」 + 다음에 누를 번호(32 한 방에 / 수량표 있으면 28→18) |
| 도면 없음 | **도면 요청 메일 제목+본문**을 `_도구결과\요청분기\{날짜}\보낼메일_도면요청_{현장}.txt`(cp949) 로 써 둔다 |
| 회의록에 제안서·자료 요청 | **제안서 구성 확인 메일**(7장 안 + 필요한 자료) 을 같이 써 둔다 |

- 36번(오늘 한 방에) 안에서 42 앞에 저절로 돈다. 42 아침 한 장 **1층**에 「도면이 없어 견적을 못 만듭니다 → 메일 본문 준비됨」 이 빨간 줄로 뜨고 파일 링크가 붙는다.
- 메일 본문의 **금액·수량·기한은 `[   ]` 빈칸** — 도구도 클로드도 정하지 않는다. 담당자·객실수·성급·준공일은 확정 대장(43)·현장대장에서 가져오고 없으면 「미확인」.
- 대외로 나가는 메일에 내부 사정(원가·타사 단가·내부 비판)은 넣지 않는다.
- 근거 : 프로님 2026-09-17 「견적서를 요청하면 도면이 있으면 네가 견적서를 만들고, 도면이 없으면 도면을 넣어 달라고 보고하고 … 네가 그 이메일을 작성을 해주고 비서답게」
- 39 자가 시험 **46/46 통과** (45 검사 6항목 추가, 시험자료에 `현장대장_시험.csv`).

## v40
**46 앞으로 해야 될 것 + 「제가 만들까요?」** (`t46_plan.py`, `facts.plan_*`) — 프로님 2026-09-17 "매일 아침 이걸로 보내. 서류 만들 게 있으면 나한테 만들라고 하지 말고 네가 만들까요 하고 물어봐. 체계를 바꾸라고."

- 판단은 클로드(회의록을 읽고 받은답 `앞으로,현장,때,등급,할일,누가,왜,만들기`), 배달·표시·완료는 파이썬.
- 대장 `_도구결과\_대장\앞으로할것.csv` (같은 현장·할 일 중복 안 됨, 완료는 상태=완료로 남김).
- 42 아침 한 장 : 1층에 오늘·지남·3일 내 줄(보라 글씨 = **제가 ○○ 만들까요?**), 2층 현장 카드에 「앞으로 해야 될 것」 표, 타일 「제가 만들까요?」, md 「## 제가 만들까요?」·「## 앞으로 해야 될 것」 절.
- 35 아침 메일 : 제목 「오늘 할 것 n건 · 제가 만들까요 n」 + 요약 엑셀 3시트 첨부.
- 46번 : 목록 → 끝난 번호 입력 → 완료. 받은답 `앞으로완료,현장,할일 일부` 도 됨.
- 39 자가 시험 **55/55 통과** (v40 검사 9항목, 시험자료 `앞으로할것_시험.csv` + 받은답 `앞으로`·`앞으로완료` 줄).

## v41
**47 견적 보낸 곳 · 찾아갈 곳** (`t47_visit.py`, `facts.quote_*`) — 프로님 2026-09-18 "견적을 보낸 곳을 찾아가 봐야 된다고, 방문해야 할 것을 메일에 적게 해."

- 대장 `_도구결과\_대장\견적발송.csv` (보낸날·현장·받는곳·담당자·금액·마지막방문·방문횟수·상태·메모)
- **보낸 뒤 3일** 안 가보셨거나 **다녀온 뒤 14일** 지나면 → 42 아침 한 장 1층(파랑 「찾아가실 곳」)·타일·md 「## 찾아갈 곳」·35 메일 제목(「찾아갈 곳 n곳」)+엑셀 첨부에 **매일**
- 넣는 길 : 받은답 `견적발송,현장,받는곳,담당자,금액,메모` / 47번 / csv 직접. 다녀오시면 47번 번호 또는 `방문,현장,메모`
- 수주·탈락은 상태칸에 적으면 목록에서 빠진다
- 39 자가 시험 **61/61 통과** (v41 검사 6항목, 시험자료 `견적발송_시험.csv`)
