# 새 창 첫마디 (이 파일 전체를 복사해 새 창에 붙여넣으십시오)

【KM 도구】 인수인계 읽고 이어서 해 — 2026-09-18 v27

## 새 창이 가장 먼저 할 일
1. 스킬 `km-32-session-start` 적용. GitHub `gamakatu-PM/mic-pm` 브랜치 `claude/cowork-suggestions-bohllt` 의 `배포4/` 가 최신 정본.
2. `3_공통사용\인수인계함\1_결정대기_v7_260916.xlsx` 의 **답변칸이 채워진 줄**부터 처리.
3. 「이번창_결정사항」 15줄은 **뒤집지 않는다.**

## 지금 상태 (전부 프로님 PC에서 확인됨)
- **도구 35개 + 엔터=오늘 한 방에** (v19). `2_KM도구\시작.py` → 번호 선택
- 경로 6개 모두 자동 인식. 현장 9개 잡힘
- 폴더: `_현장비서` / `2_KM도구` / `3_공통사용` + `_원틀`·`plaud`·`_코드`·`Claude outputs`
- **갱신은 98번** — zip 을 다운로드 폴더에 받아두고 98번만 누르면 코드·엑셀·지도까지 갈아끼움

## 절대 하지 말 것
1. `_현장비서`·`_원틀`·`plaud`·`26년`·`1.현장` **이름 바꾸기** — 회의록 코드가 그 이름을 씀(26번 검사 확인)
2. 폴더 통째 zip 보내기 — 프로님이 정리해 둔 것이 헛수고가 된다. **갱신은 98번용 zip 으로**
3. 회의록 프로그램(`_현장비서`) 코드 건드리기 — 다른 창이 만든 것

## 2026-09-16 추가 (v10)
- **27번 도면 수량 뽑기** 만듦. 토큰 0. DXF 블록 개수 + PDF 글자로 센다
  - 도면 넣는 곳 : `3_공통사용\도면\_여기에_넣으십시오\` (27번이 없으면 스스로 만든다)
  - 사전 : `3_공통사용\도면\기호사전.csv` (품목칸에 「무시」를 넣으면 안 센다)
  - **수량은 도구가 정하지 않는다** — 블록기준/글자기준을 나란히 내고 채택근거를 적는다
  - `K+DM`·`L-SW` 처럼 쪼개서 맞춘 것은 별도 표로 확인받는다
  - DWG 는 ODA File Converter 가 깔려 있으면 자동 변환, 없으면 「DXF로 저장」 안내
  - 사진·스캔 PDF 는 파이썬으로 못 센다 → 클로드에게 줄 목록만 뽑는다
- 무거운 부품(ezdxf·pymupdf)은 `common.ensure_pkg` 로 **27번 누를 때만** 받는다. 시작.py 는 그대로 빠르다
- 98번이 이제 `사용법.txt` 도 갈아끼운다 (다음 갱신부터 적용)

## 2026-09-16 저녁 추가 (v11~v17)
- **27번 수정** : 읽기 전에 묻지 않는다 / NOTE 표 그대로 읽기 / 글자횟수 채택 금지(규칙 위반이었음) / 결과 자동 오픈(97번) / **v13~v16 끝부분 `made` 버그 수정(v17)**
- **28 단가 붙이기** : 단가장 xlsx 그대로 읽음, 4단계 매칭, 후보 2개↑면 비움. CB = 자재×(1+조립비율)×대수
- **29 단가장 채우기** : 배선도 → 없는 모듈 → 단가장 vN+1 주황 줄(실행가 빈칸). 모듈무시.csv
- **30 완성품** : 빈 곳 → `_클로드부탁서_{현장}.md` / 답 csv → `받은답\` → 자동 반영
- **31 도면 접수·판 비교** : `도면\{현장명}\` 해시 대장, r1/r2 증감
- **32 현장 한 방에** : 31→27→28→29→30 자동(common.AUTO)
- 앵커호텔 : 도면 NOTE 수량표 v1 + 실행견적 산출 v2(2000M, 광희동 CB 구성, 실행 126,197,060 / 견적 227,154,708). **CB 배선도·외함·배수 미확정**
- 드라이브 `CB모듈_단가장_v70` **같은 이름 2개(중복)** — 정본 미정
- **견적서 원틀이 PC에 없다** → 12번이 못 돈다. 드라이브 광희동1가_견적서_Rev1(일반형)/양양쏠비치(층별형)

## 2026-09-16 밤 추가 (v18) — 프로님 지시 「시간이 걸려도 캘린더·단가장·대시보드 다 완성」
- **33 현황판** : 모든 도구 결과 한 장 → `{base}\_현황판.html` + **`인수인계함\_현황판.md`(새 창의 클로드는 이것부터 읽는다)**. 32번 끝에 자동
- **34 캘린더** : 납기 역산·결정대기 ★급함·도면 접수 → `.ics` (아웃룩 더블클릭 / 구글 가져오기). 계정 없이 파일만
- **35 아침 메일** : 설정.ini [메일] + SMTP + schtasks 05:00. `2_KM도구\아침메일.py` 런처. **프로님이 앱 비밀번호를 넣어야 동작**
- 단가장 v68 조회본(212줄)을 xlsx 로 만들어 전달 → `3_공통사용\단가장\` 에 넣으면 28·29·30 이 돈다 (실제 212줄로 32번 6단계 통과 확인)
- 단가장 머리글 판정 강화(설명 줄 오인 방지), 판 번호 날짜 꼬리 중복 수정
- 미완 : ODA 미설치라 DWG 직접 읽기 미검증 / 35 실제 발송 미검증(SMTP 환경 없음) / 견적서 원틀 없어 12번 미검증

## 2026-09-16 밤 v19 — 「내가 좋아하는 방식을 검사해서 계획을 실행해」
- 검사표를 **km-34-pro-preferences 스킬**로 만들었다 (3_스킬_저장대기). 새 창은 만들기 전에 이 7줄로 검사
- 검사에서 나온 실행 : **시작.py → 엔터 = 36 오늘 한 방에** (새 zip 자동 적용 / 받은함 `[현장명]` 자동 분류 / 새 판 현장만 27~30 / 33 현황판)
- 프로님 손 3번 → **① 받은함에 넣기 ② 엔터 ③ 부탁서 던지기**
- 시험 : 받은함 3장 → [앵커호텔] r3(+8 ×3), [쏠비치양양] 새 폴더 r1, 현장명 없는 1장은 그대로 둠 → 현황판까지 통과

## 2026-09-16 밤 v20~v21 — 「했던 것을 못 하는」 사고와 대책
- 사고 : 앵커호텔 실행산출을 클로드가 손으로 3번 만들며 3번 다 서식이 달랐다(자체 서식 → 견적서 서식 → 6시트). 프로님: "파이썬이면 명확히 값을 넣고 해야 할 것만 하게 할 수 있지 않아?"
- 대책 1 : **실행산출 6시트는 28번 코드(execsheet.build)만 만든다.** 클로드가 손으로 xlsx 를 그리지 않는다. 정답본(주일능 v7)을 `_원틀\정답본\` 에서 복사해 값만 채운다
- 대책 2 : **37번 검수** — 정답본 대조(시트·머리글·참조·검산·노란칸 → 라벨·재계산·수량). 28번이 끝에 자동 호출. 클로드도 xlsx 를 보내기 전 반드시 37번 결과를 붙인다
- 대책 3 : 단가장은 **모든 시트**를 읽는다(총괄 우선, 직접단가 등 포함) — 「첨부한 값을 안 읽어 빈칸」 방지
- 4~6버튼 = 통신가 규칙을 별칭 기본값에 반영 (km-fixture-cost)
- 정답본 실행산출 = 주일능_연합기숙사_실행산출_v7 (프로님: "내가 생각했던 틀은 그것")

## 2026-09-16 밤 v22 — 견적서(고객용)도 코드가 만든다
- **quotesheet.build** : 정답본 `광희동1가_견적서_Rev1.xlsx`(갑지+내역서) 를 `_원틀\정답본\` 에서 복사해 값만 채움. 28번 끝에 실행산출 다음으로 자동 생성 → `{현장}_{날짜}_견적서_v1.xlsx`
  - 내역서 : 1.중앙관리 / 2.객실관리(CB 라인 + 서브라인 -제조사/-제질/-기능/-모듈×n, 외함 [규격확인], 기구물, 노무 3종은 노무비 열) / 소계 2 / 조건문구 7줄 / 합계
  - 갑지 : 날짜·공사명·수신·一金 한글(CONCATENATE)·합계 셀 참조
  - **원가는 절대 안 넣는다.** 견적단가 = ROUND(실행 × 견적배수). 단가 없으면 빈칸 노랑 [단가확인]
  - 37번 검수 자동(정답본 등록부 `_정답본.csv` 의 「견적서」 줄, 필수시트 갑지|내역서)
- 시험(앵커호텔 수량표 + 단가장 v68 212줄) : 실행산출 6시트 통과 / 견적서 통과
- **프로님 PC 에서 해야 할 것** : `광희동1가_견적서_Rev1.xlsx` 와 `주일능_실행산출_v7.xlsx` 를 `3_공통사용\원틀\정답본\` 에 넣기 (zip 에는 회사 원가 파일을 넣지 않으므로 따로 드린 파일)
- 미해결 그대로 : 앵커호텔 CB 배선도 미판독(DWG, ODA 없음 → CB 구성은 광희동 잠정) / 노란칸 7~9개 / 배수 확정 / 단가장 v70 중복
- **이 클라우드 창은 프로님 OneDrive·PC 폴더를 못 본다.** 읽을 수 있는 곳 = Google Drive(연결됨) 와 대화창 첨부뿐. PC 안의 도면은 32·36번(파이썬)이 읽는다

## 2026-09-16 밤 v23 — 실행산출 정답본은 「주일능_실행산출_v5_260828」 (프로님: "지금 첨부한 것이 기준이야")
- **v7 은 기준이 아니다.** 정답본 폴더에는 v5 만 둔다. 37번 등록부(_정답본.csv) 실행산출 줄 = `주일능_실행산출_v5` (옛 등록부는 37번이 스스로 승격)
- v5 가 v7·코드 초판과 다른 점 (코드 v23 에 반영 완료) :
  1. **3.견적↔실행 대조** : 제목 줄 없이 1행이 머리글. 열 순서 **실행단가·실행금액 → 견적단가·견적금액 → 이윤·이윤율 → 견적 2블록(L~O 견적단가·견적금액·이윤·이윤율)**. 비고 열 없음. 구역마다 라벨 없는 소계 줄, 끝에 「TOTAL 」 초록. NOGO·검산 줄은 대조 시트에 없고 **0.자가진단 B5 = 대조!I{TOTAL} − NOGO 숫자**
  2. **2.CB 실행** : 「▷ 참고 — 견적서 CONTROL BOX 단가」 다음에 **「 * 견적가 - 실행」 주황(FF9900) 줄**. 품목명에 형번, 「견적서 표기(원문)」 에 원문, 비고(H, 너비 70)에 판단 근거
  3. **1.입력판** : 미확인 실행단가 아래 **「■ 도어락 파트 — 직접단가 품목」** 묶음 별도. 노란칸 색 **FFF2A8**
  4. **4.확인 목록** 금액 영향 = 「299 × 단가」 처럼 실 수를 붙여 쓴다
- **확인 못 한 것** : 대조 L~O 두 번째 견적 블록의 뜻(v5 는 CB 480,000 / 450,000 두 값 — 조정견적? 계약가?). 코드는 첫 블록과 같은 값(=H)으로 두고 자가진단에 자가신고. v5 O9 셀에 `#REF!` 가 있음(프로님 원본 그대로, 손대지 않음)
- 앵커호텔 실행산출을 v5 틀로 다시 생성 → 37번 검수 「통과」(정답본 v5, 시트 닮음 100%)

## 2026-09-16 밤 v24 — 「연합기숙사 것을 기준으로 값을 넣으면 되지, 왜 비워두냐」 (프로님)
- 오해 정리 : 조립비(30%)는 v21 부터 이미 2.CB 실행에서 자동 계산되고 있었다. 비어 있던 것은 **단가장 v68 에 줄이 없는 5개**(외함·노무 3종·퓨즈 1A) + 온도형 2개였고, 제가 「선례 값을 넣지 않는다」 는 규칙을 너무 좁게 적용해 비워 둔 것.
- **새 규칙(코드 v24)** : 단가장에 없으면 **선례 = 정답본 실행산출(연합기숙사 v5)에 적힌 숫자**를 넣고 노란칸으로 둔다(확정은 프로님). 선례에도 없는 것만 비운다. 후보가 여럿이라도 값이 전부 같으면 채택(퓨즈 1A=50). 계열 매칭은 후보 2개 이상일 때만(BSP 온도형에 전등형 단가 붙던 사고 방지).
- 앵커호텔 남은 빈칸 **2개뿐** : BED SIDE PANEL(온도+USB+유니버셜) / LIGHT SWITCH+온도조절기(버튼 수). 단가장·연합기숙사·광희동·에피소드남산 어디에도 2000M 온도형 실행가가 없다 → 프로님 숫자 필요.

## 2026-09-16 밤 v25 — 「앵커호텔 정보는 지우고, 연합기숙사 것으로 앵커호텔을 만들어라」 (프로님)
- 이전 앵커호텔 산출물(손으로 만든 v1·v2, 광희동 CB 구성 기반 코드 생성본)은 **전부 폐기**. 앵커호텔은 이제 **연합기숙사 v5 를 틀·값·CB 구성의 기준**으로 코드가 만든다
- **CB 구성 = 정답본 2.CB 실행 12종 그대로**(MAIN 보드·MCB 40AT·RCBO×6·LRY101A·RL-30SAP4P·POWER TRANS·SSR 80A·퓨즈 0.5A/5A·CHIME·부스바·소형 SSR). 단가장에 없는 것은 선례실행가(정답본 값). CB구성.csv 는 정답본에서 자동 생성(있으면 그대로) → 앵커 1대당 295,295(자재 227,150 × 1.3)
- 노무 3종·외함 선례값이 **견적서에도** 들어간다(노무비 열 54,000/54,000/36,000, 외함 177,120)
- **37번 : 노란 빈칸이 남아 있으면 「통과」 가 아니라 「이상 - 빈칸 n개」**. 프로님 지적: "이런 것을 검수해서 통과한 것인가" — 이전엔 서식만 보고 통과라 했음. 잘못이었다
- 금액 칸이 비어 보인 것 = 미리보기가 수식을 계산 안 한 것. 파일에 fullCalcOnLoad 를 넣어 엑셀이 열 때 계산한다
- 앵커 남은 빈칸 2개 : BSP(온도+USB+유니버셜) / LIGHT SWITCH+온도조절기 → 프로님 숫자

## 2026-09-16 밤 v26 — 「같다고 생각하나? 챙피하게」 : 금액 칸이 전부 비어 보인 사고
- 원인 : openpyxl 이 만든 xlsx 는 **수식만 있고 계산값이 없다.** 프로님이 대화창에서 내려받은 파일은 엑셀 「보호된 보기」 로 열려 다시 계산을 안 하므로 금액·소계·합계가 전부 빈칸으로 보였다. 연합기숙사 v5 는 엑셀이 저장한 파일이라 값이 들어 있었다. → 서식이 같아도 **값이 안 보이면 같은 것이 아니다.**
- 대책(코드 v26) : `common.finish_xlsx()` — 저장 직후 ① 윈도우+엑셀이면 엑셀 COM 으로 한 번 계산해 저장 ② 아니면 37번 Calc 로 파이썬이 계산한 값을 파일(`<v>`)에 넣는다. execsheet·quotesheet 둘 다 호출. IF/COUNTBLANK/CONCATENATE 몇 칸은 편집 시작 시 계산.
- **클로드가 xlsx 를 보내기 전 반드시 `data_only=True` 로 다시 열어 합계 칸에 숫자가 있는지 본다** (37 검수와 별개의 마지막 확인)
- 앵커 실행산출 v4 : CB 1대 295,295(외함 포함 393,695) / 실행 TOTAL 197,134,460 / 견적(×1.8) 354,842,028 — 온도형 2품목은 0 으로 잡힌 값

## 2026-09-16 밤 v27 — 「견적서도 광희동 것을 기준으로, 그 틀에 앵커를 넣어라. 다른 견적도 마찬가지」 (프로님)
- **원칙(모든 현장 공통)** : 견적서 = 광희동1가_견적서_Rev1 의 틀·명칭·규격·서브라인 그대로, 바뀌는 것은 현장명·수량·CB 구성·단가뿐. 클로드/코드가 새 이름을 짓지 않는다
- 코드 v27 (`quotesheet.quote_name / labor_name / cb_sublines`) :
  - 명칭 광희동식 : 입구 INDICATOR / KEY SENSOR & D/M PLATE / BED SIDE PANEL / LIGHT SWITCH N버튼 / 비상호출 BUTTON / 방문자 알림램프 / RF DOOR LOCK / OPERATION PC / FLOOR INDICATOR PANEL
  - 규격 = **형번 — 설명** (CI-2000M / KD-2000M / BSP-2000M — 전등+USB+유니버셜 / LS-2005 (통신) — 욕실 / FIP — 층당 1대). 단가장 긴 이름을 규격에 넣지 않는다
  - CB 서브라인 : 제조사·제질·기능 3줄 + MAIN 보드 / MCB + 들여쓴 RCBO × 6 / 릴레이 (+ 5A 퓨즈) / POWER TRANS (+ 퓨즈 0.5A) / … 퓨즈는 부모 줄에 붙인다. **2대째부터 구성이 같으면 「-CB1과 내부 구성 동일」 한 줄**
  - 노무 명칭 : 약전 결선(객실CB약전,객실디바이스,약전외) / 객실 C/B 배전판 취부,객실 디바이스 백커버/기구물 설치 / 시운전비(약전접속,현지조정) MK-TSET
  - 비고 : 단가 있는 줄 「견적가 기준 (실행×1.8)」, 선례 줄 「[선례단가] 확정 전」, 없는 줄 「[단가확인]」. 공사명 「{현장} 신축공사 (견적가)」
  - 갑지 一金 = 파이썬이 합계를 알므로 광희동처럼 **한글 금액 글자**로 (수식 아님)
- 앵커호텔 견적서 v5 : 합계 354,842,028 (온도형 2품목 0 포함). 이전 앵커 견적서 v1~v4 폐기

## 2026-09-16 밤 v28 — 프로님 「기본 원칙」 + 「정말 편한 방법」
### 기본 원칙 (스킬 km-31 v7 · km-34 v2 에도 넣음. 새 창은 이것부터)
1. **틀 = 연합기숙사 v5(실행산출) · 광희동1가 Rev1(견적서) 정답본 그대로. 내용 = 도면을 검토한 결과.**
2. **이미 만든 결과를 버리고 새로 만들어 틀리게 하지 않는다.** 값만 갱신, 새 버전으로. (오늘 앵커 실행산출을 4번 다시 만든 사고)
3. **계통도에 수량이 없으면** 파이썬이 센 값(블록)은 검토용. 맞는지 검토하되 **프로님께 수량표를 요청한다** (30번 부탁서 「수량표 확인 요청」 절 자동, 답 `수량,품목,수량,근거` → 정답 취급)
4. **파이썬 위주. 안 되거나 부족한 것만 클로드에게.**
### 정말 편한 방법 (v28) — 프로님 손 2번
- 사고 : "zip 받아서 시작 눌러 숫자 누르고… 너무 불편"
- **38번 자동 실행 등록** : 작업 스케줄러 「KM_자동_로그온」(로그온 2분 뒤) + 「KM_자동_30분」(v29, 2시간→30분; 급하면 KM_자동.py 더블클릭 즉시). 런처 `2_KM도구\KM_자동.py` (pythonw, 검은 창 없음) → `t36.run(quiet=True)`
- **자동 갱신** : `t98.remote_version()` 이 GitHub raw `common.py` 의 VERSION 을 읽고, 새 판이면 `KM_latest.zip`(저장소 루트, 항상 최신) 을 `다운로드\KM\` 에 받아 98번 apply → 스케줄러 모드면 `os.execv` 로 스스로 재시작. 저장소는 public 이라 토큰 불필요. 설정.ini `[갱신] url=` 로 바꿀 수 있음
- quiet 모드 : 묻지 않고, **새 판이 생긴 현장이 있을 때만** 현황판을 띄운다
- 남는 손 : ① 도면 넣기 ② 부탁서 던지기. **클로드는 매 판 zip 을 `KM_latest.zip` 으로도 커밋해야 한다** (안 그러면 자동 갱신이 멈춘다)
- 미검증 : schtasks 등록·pythonw 실행·os.execv 재시작 전부 프로님 PC 실물 미검증

## 2026-09-16 밤 v30 — 프로님 「시간 주기는 싫다. 파일 넣는 곳에 시작 같은 바로가기」
- 스케줄러(38번)는 **원하실 때만**. 기본은 **도면 폴더의 바로가기 2개** (`common.make_shortcuts`, 27·36번이 자동 생성)
  - `3_공통사용\도면\★도면넣고_여기클릭.py` = `시작.py 36` (번호 미리 입력). 새 zip(다운로드 폴더)·정본 새 판 자동 적용 → 도면 처리 → 현황판
  - `3_공통사용\도면\★번호입력.py` = `시작.py --ask` → 번호 하나 받아 실행 (클로드는 "★번호입력 눌러 38" 식으로 안내)
- `시작.py <번호>` / `시작.py --ask` 지원 (`menu.run_number / ask_and_run`). 메뉴 번호 정렬: **36 = 오늘 한 방에, 37 = 검수, 38 = 자동 실행** (v29까지 메뉴엔 검수가 36으로 찍혔음 — 정정)
- v31 : 프로님 "왜 또 클로드 폴더 안에 있어야 하냐" → 바로가기 안에 **시작.py 절대 경로**를 적어 넣어 어디(바탕화면 등)에 두어도 된다. 이름 `★KM_도면넣고_여기클릭.py` / `★KM_번호입력.py`, **바탕화면과 도면 폴더에 자동 생성**(시작.py·27·36 실행 시). 폴더를 옮기면 위쪽 7단계 탐색으로 다시 찾는다

## 2026-09-16 밤 v32 — 「더 보완할 것·오류 찾아」 → 전체 흐름 자가 시험에서 잡힌 오류 5개 + 39번 자가 시험
- 잡힌 오류(전부 코드 v32 에서 수정, 시험으로 재현·통과 확인)
  1. **27번이 같은 도면의 Rev0·Rev1·Rev2 를 전부 읽어 NOTE 수량을 3배로 합산** → `latest_per_drawing()` : 같은 도면번호(T-1101)는 최신 판(Rev 번호 > 날짜 > 수정시각)만 읽고 이전 판은 「(이전 판)」 으로 표시
  2. **28번이 수량표 csv 에서 가장 긴 글자칸을 품목으로 잡아 파일명이 품목이 됨** → 머리글(내용/품목·수량)로 열을 잡는다
  3. **28번 CB 대수 기본값이 항상 0** (`rooms_of` 가 문자열 '0' 을 줘 참으로 평가) → 정수 변환 + 실당 품목(입구 INDICATOR 등) 수량으로 추정(`guess_rooms`)
  4. **31번이 새 파일 하나(CB 배선도)만으로 판을 만들어 r4=1품목** → 판은 현장 전체 파일의 최신 판 묶음으로 만든다
  5. **37번 오탐** : CB 타입 1개인 현장에서 「CB2 수량」 머리글 없음을 잘못으로 / 소계·실행가 줄을 「단가 빈칸」 으로 → CB\d 열 제외, 기호(▣■◆▷*)·실행가 줄 제외. 1.입력판 라벨을 정답본과 글자까지 같게(「■ 수량 (견적서에서 읽음)」, 도어락 머리줄 항상)
  + 영문 NOTE 별칭(ENTRANCE INDICATOR·EMERGENCY·bath 5/6·VISITOR) 추가, 별칭.csv 는 기본줄 중 빠진 것만 뒤에 붙임(`seed_merge`)
- **39번 자가 시험** : `2_KM도구\시험자료\`(합성 PDF·DXF + 가짜 단가장) → 임시 폴더에서 36번 전체 → 16개 기대값 대조. 정답본 폴더가 있으면 37 검수까지. **클로드 규칙 : zip 을 만들기 전에 39번 「전부 통과」 를 찍고, 그 결과를 전달보고 ④ 에 적는다.** 프로님 불안("한 달 뒤 또 이상한 짓")의 답 = 스킬 원칙 + 정답본 검수 + 이 자가 시험 3중
- 메뉴 : 36 오늘 / 37 검수 / 38 자동실행(원하실 때만) / 39 자가 시험

## 2026-09-17 v33 — 「회의록(PLAUD)과 도면 흐름 잇기 시작하자」 (프로님)
- **PLAUD MCP 확인됨** : 이 세션에서 `list_files → get_note` 로 9/10 「일능 홍부장 협의록」 노트를 읽으니 **v10 결과(【1부】■현장 연합기숙사 … ■수량·규격 변경 … ■할 일)가 그대로** 나왔다. 앱에서 「커스텀 생성」을 한 통화만 노트가 있고(9/16 통화 8건은 비어 있음 = 아직 안 만드신 것), 「자동 생성」은 여전히 v10 이 아니다. 즉 **프로님 손 2번(앱에서 글 복사 → 메모장 저장)은 클로드가 대신할 수 있다.** 클로드 절차 : 날짜 범위로 `list_files` → 노트 있는 것만 `get_note` → 【1부 가 있는 노트를 **파일명 = PLAUD 제목** txt 로 저장해 대화창에 준다(내용은 손대지 않는다). 프로님은 `다운로드\KM_회의록받는함\` 에 저장만.
- **40번 회의 연결(`t40_meeting.py`)** — 36번 첫 단계(자동 갱신 다음)에서 조용히 돈다. 메뉴 40. ① 받는함 txt → `_현장비서\1.여기에_v10결과_넣기` (같은 이름이면 _2 붙임, 덮어쓰지 않음) → `시작.bat` 띄움(설정.ini `[회의] 실행명령` 있으면 그것을 조용히) ② `plaud\…\원문.txt` 의 「■ 수량·규격 변경」 + meta.json 의 '변경' 목록 → `_도구결과\회의연결\{날짜}\{현장}_회의변경수량.csv` ③ 30번 부탁서 「회의에서 바뀐 수량·규격」 절(현장명은 ■현장 값과 도면 폴더명을 공백·괄호 빼고 포함관계로 맞춤) + 현황판 **⑦** (최근 30일) ④ `_현장비서` 코드(.py·.bat·.txt·.ini) → `인수인계함\회의록코드_백업\{날짜}_{해시}\` + `다운로드\KM_회의록코드_{날짜}.zip` (해시 같으면 다시 안 만듦)
- **수량은 여전히 도구가 정하지 않는다.** 회의 변경 줄은 확인용이고, 반영은 프로님 답 `수량,품목,수량,회의 YYMMDD` 로만.
- **저장소에는 회의록 코드가 없다** (프로님 PC `_현장비서\코드` 가 정본). 40번이 만든 `KM_회의록코드_*.zip` 을 한 번 첨부해 주시면 `현장비서_도구/회의록코드/` 에 넣어 98번 갱신 대상으로 만든다. km-30 은 파일명을 km_run·km_meeting_v1_6·km_workorder 라 하고 km-19 는 km_meeting_v1(v1.3)·km_index_v2(v2.1)·km_run 이라 하여 **어느 쪽이 실물인지 미확인** → 백업이 그 답을 준다.
- **`_현장비서` 코드는 여전히 건드리지 않는다.** 40번은 파일을 옮기고 bat 을 띄우고 읽기만 한다.
- 39 자가 시험 **21/21 통과** (회의 5항목 추가 : 변경 2줄 / 부탁서 절+330 / 받는함 1개 이동 / 코드 백업 / 현황판 ⑦). 시험자료에 `plaud\1.현장\{앵커호텔,연합기숙사}\회의록\…\원문.txt`, `받는함\`, 가짜 `_현장비서\` 추가. 정답본 xlsx 가 없으면 37 검수 항목은 「생략」 으로 통과(폴더만 있고 xlsx 없을 때 오탐하던 것 수정)
- 미검증 : 프로님 PC 에서 시작.bat 이 os.startfile 로 뜨는지 / `_현장비서` 실제 폴더 구조(`1.여기에_v10결과_넣기` 이름) / 회의록 폴더 위치가 `plaud\26년\1.현장\{현장}\회의록\` 인지 (아니어도 원문.txt 를 재귀로 찾으므로 동작)
- 다음 : ① 프로님이 KM_회의록코드 zip 첨부 → 저장소 등록 ② 「PLAUD 가져와 9/16」 시험 1회 ③ 실행명령 확정(km_run.py 인수를 코드에서 확인한 뒤) → 시작.bat 창도 없어짐

## 2026-09-17 v33-2 — 「총괄적으로 확인하는 방안」 (프로님) → 41번 총괄 점검
- **41번 `t41_totalcheck.py`** : A 판·갱신(지금 판·GitHub 정본 판·다운로드 새 zip·부품) / B 폴더·경로(8경로·`_현장비서` 안 구조·정답본 2개·단가장·견적 원틀·바로가기·받는함) / C 회의록(대기함·처리완료 대 회의록 폴더·마지막 회의 며칠 전·30일 변경 줄·코드 백업) / D 도면→돈(현장별 도면 수·판·수량표·실행산출·견적서 + **37 검수 판정 자동**(정답본 있을 때, 묻지 않음)·부탁서 항목·받은답 대기) / E 39 자가 시험 / F 38 스케줄러(참고). 등급 통과·주의·실패·미확인, **실패·주의마다 조치 한 줄**, 맨 위 「지금 할 것」.
- 산출 : `_도구결과\총괄점검\{날짜}\총괄점검.html·csv` + `base\_총괄점검.html` + **`인수인계함\_총괄점검.md`(클로드용)**. 36번 끝에 `build(quick=True, quiet=True)` 가 돌아 md 가 항상 최신 → **새 창은 `_현황판.md` 와 `_총괄점검.md` 둘을 먼저 읽는다.**
- 검사 하나가 터져도 「미확인」 으로 남기고 다음으로 간다(`_try`). 39 검사 22항목(총괄점검 md 추가). 21/21 통과(정답본 없는 환경 기준).
- 클로드 쪽 총괄 확인은 이 md + 현황판 md 를 읽고 「실패·주의」 만 답하는 것으로 고정. 프로님이 화면을 찍어 보내실 일이 줄어든다.

## 2026-09-17 v34 — 「B안 v3로 해」 + 「내가 고친 것이 결정 사항이 되고 검색되게」 (프로님)
- **사고** : 샘플에서 연합기숙사를 「빽커버 진행 중」 으로 그렸는데 실제는 **외함만 납품 중**. 도구가 추측한 공정을 확정처럼 보이게 한 것이 잘못. → 42번은 공정을 **확정(43번 값) / 추정(준공일 역산, 빗금) / 미확정(빨강)** 세 가지로 반드시 구분해 표시한다. 추측을 확정처럼 그리지 않는다.
- **확정 대장 `facts.py`** : `_도구결과\_대장\확정사항.csv` (일자·현장·항목·값·근거·누가·상태). `add()` 는 새 줄을 위에 넣고 옛 줄을 상태=이전 으로(덮어쓰기 없음). `get(site,item)` 은 현장 포함관계로 맞춘다. **어떤 추정보다 우선.** 넣는 길 : 43번 대화식 / 받은답 `확정,현장,항목,값,근거`(30 apply_answers, ANS_KIND 에 '확정') / csv 직접.
- **42번 아침 한 장(7층)** `t42_morning.py` : 업무판 파일을 plaud·biseo 아래에서 이름(00_미결_할일·00_작업의뢰서_대기·00_카톡_보낼문구·회의록_색인)으로 찾고 **머리글 낱말(현장·기한·내용·담당·부서)로 열을 잡는다**(열 순서 몰라도 됨). 회의록 원문.txt 에서 안건·결정사항·조치사항·할 일·리스크·★대외금지·타부서·수량변경 절을 읽는다. 도면(t33 site_rows/request_rows)·수금(t13 money_rows)·결정대기(t33 urgent_rows)·41 빠른 점검을 합친다. 산출 `base\_아침한장.html` + `인수인계함\_아침한장.md`(**확정 표가 맨 위** → 새 창이 먼저 읽음). 36 끝·35 메일(`_morning_or`)이 쓴다.
- **43번 확정 입력·찾기** `t43_confirm.py` : 현장/항목(번호)/값/근거. 「?낱말」 찾기. 끝나면 42 재생성. **찾기는 파일 안에서 도니 토큰 0** (프로님 질문 "검색 쓰면 사용량 폭주?" → 아니오. 클로드에게 말로 물을 때만 토큰).
- 39 자가 시험 **27/27** (아침 한 장 html/확정 외함/업무판 읽기/회의 이력/받은답 확정→대장/md 확정 표). 시험자료 : `plaud\00_*.csv`, `단가장\받은답\시험_확정.csv`, `확정사항_시험.csv`.
- 미확인 : 업무판 csv 실제 머리글(낱말 매칭이라 대개 되나 확인 필요) / 업무판 파일 위치 / 10번 발행대장 파일명(`의뢰서발행대장`·`_대장\*의뢰서*`로 찾음) / 결정 번복 감지는 낱말(변경·대신·취소·철회·→) 기준 단순판.

## 2026-09-17 v35 — 「도면 안에 연도를 구분해야 되서 26년 폴더를 만들어 도면을 넣었어」 (프로님)
- **사고(잡음)** : `t31_intake.sites()` 가 도면 폴더 **바로 아래**만 현장으로 봤다. `3_공통사용\도면\26년\{현장명}\` 구조가 되면 **「26년」이 현장 이름이 되고 실제 현장은 하나도 안 읽혔다.** 프로님이 말씀해 주셔서 잡음.
- 고친 것 : `year_key()`(26년·2026·26·2026년 → 연도 정수) / `year_dirs()`(최신 연도 앞) / `newest_site_root()` / `sites(with_year=True)`. **연도 폴더 안 → 연도 폴더 밖** 순서로 훑고 같은 현장 이름은 **최신 연도 것 하나만**. **연도 폴더 자체는 현장으로 잡지 않는다**(`_scan` 에서 `year_key(d)` 걸러냄 — 1차 수정에서 26년이 현장으로도 잡혀 `26년\_도면대장.csv` 가 생긴 것을 자가 시험으로 재차 잡았다).
- 받은함 분류 : 이미 있는 현장이면 그 폴더(연도 폴더 안이어도) 그 자리로, 새 현장이면 **최신 연도 폴더 아래**에 만든다.
- `process_site` 결과에 `'dir'` 추가 → **t36 이 `dwg_root()+현장명` 으로 경로를 다시 조립하던 것을 실제 폴더로 교체**(연도 폴더 안 현장이 32번으로 안 넘어가던 버그).
- 31번 화면 : `연도 폴더 1개 : 26년 (새 도면은 26년\ 로 들어갑니다)` / `현장 2개 : 쏠비치양양, 앵커호텔(26년)`. 도면 폴더 `_읽어보세요.txt` 안내에도 연도 폴더 절 추가.
- 시험자료를 `도면\26년\앵커호텔` + `도면\쏠비치양양`(연도 폴더 밖) **두 구조 섞어** 두도록 바꿈. 39 자가 시험 **30/30** (연도 폴더 안 현장 찾음 / 밖 현장도 찾음 / 26년을 현장으로 안 잡음 3개 추가).
- 교훈(스킬에 넣음) : **프로님이 폴더 구조를 바꾸시면 도구가 따라가야 한다. 폴더를 바꾸지 말라고 하지 말고 코드가 알아보게 만든다.**

## 2026-09-17 v36 — 「도면\현장명\26년 구조야」 (프로님) + 41번에 도면 구조 표시
- 프로님 실제 구조는 **연도 폴더가 현장 폴더 안**(`도면\{현장명}\26년\`)이었다. v35 는 그 반대(`도면\26년\{현장명}\`)를 고친 것.
- 파일 찾기는 `D.gather()` 가 `os.walk` 라 이미 됐지만 **옛 연도(25년) 도면까지 같이 세어 수량이 부풀 위험**이 있었다 → `site_year_dirs()` / `_old_year()` / `skipped_years()` 로 **최신 연도 폴더 + 연도 폴더 밖만** 읽고 옛 연도는 건너뛴다(`drawing_files`·`qty_files` 둘 다). 31번 화면에 「26년\ 만 읽음 / 보관용으로 건너뜀 : 25년」.
- **41 총괄 점검 B 구역**에 도면 구조 3종 추가 : 도면 폴더 구조(연도 폴더 목록·새 도면 들어갈 자리) / 현장별 「읽는 위치 + 읽는 도면 수 + 건너뛴 연도」 / 현장 폴더 없으면 빨강. D 구역 현장 줄에도 `26년\ 도면 4개` 처럼 위치가 붙는다. → 「41 하나로 도면·경로·정답본 확인」 이 실제로 된다.
- 시험자료를 **두 구조 다** 넣음 : `도면\26년\앵커호텔\`(연도 먼저) + `도면\쏠비치양양\26년\`·`\25년\`(현장 안 연도, 25년에 ENTRANCE 999 옛 도면). 39 자가 시험 **32/32** (현장 안 연도 최신만 읽음 / 999 안 섞임 2개 추가).
- 41 실물 확인(시험자료, 정답본 넣고 full) : 실패 0 · 주의 8 (주의는 노란 빈칸·부탁서 대기 = 정상). 도면 구조 줄이 기대대로 찍혔다.

## 2026-09-17 v37 — 「플라우드 회의록은 저장만 하고… 내가 확인을 못 하니 미확인. 그것을 매일 표현해 달라」 (프로님)
- 회의록이 만들어져도 **프로님이 읽지 않고 넘어가는 것**이 진짜 구멍이었다. 파이썬은 「만들었다」 까지만 알았고 「읽었다」 를 몰랐다.
- **회의록 확인 대장** `_도구결과\_대장\회의확인.csv` (`facts.chk_path/checked/check_meeting`, 확인일·회의폴더·현장·메모·누가). 지우지 않고 쌓는다.
- **42 아침 한 장에 0층 신설** : 「미확인 회의록 n건 (저장만 되어 있습니다. 읽고 44번으로 확인)」 을 **1층보다 위, 카톡 문구 바로 아래**에 매일. 줄마다 날짜·현장·협의자·안건·결정·할 일 수·수량변경 줄 수·대외금지 수 + 회의록 워드 링크. 3일 지나면 빨강. 타일 첫 칸도 「미확인 회의록」. 층 단추에 빨간 「0층 미확인 회의록」.
- **44 회의록 확인** `t44_meetingcheck.py` : 미확인 목록을 번호와 함께 보여 주고 `1,3,5` / `a`(전부) / `o3`(3번 워드 열기). 처리하면 42 를 다시 만든다. 받은답 `회의확인,<회의폴더>,확인,메모` 도 같은 효과(30번).
- **41 C 구역**에 「미확인 회의록 n건 (3일 넘은 것 n건)」, **클로드용 `_아침한장.md`** 맨 위쪽에 「## 미확인 회의록」 절 → **새 창의 클로드가 매일 먼저 말씀드린다.**
- 39 자가 시험 **35/35** (0층 2건 표시 / 경보 줄+44 안내 / md 절 3개 추가). 실물 확인 : 44 로 1건 확인 → 「미확인 회의록 1건」 으로 줄고 대장에 기록됨.

## 2026-09-17 v38 — 「간단히 요약하되 내용 빠뜨리지 말고, 대시보드·엑셀·이메일로」 (프로님)
- 프로님이 파이썬으로 되는 것을 알고 계셨고 실제로 된다 : **읽기는 프로님, 정리·배달은 파이썬.**
- **① 대시보드** : 0층 각 줄에 `내용 펼치기 (빠진 것 없이 전부)` 접힘 추가 — 결정·조치·할 일·수량변경·★대외금지·리스크·타부서 전달 전부. 0층 아래에 「요약 엑셀로 보기」 링크.
- **② 엑셀** `_도구결과\회의록확인\{날짜}\미확인회의록_요약_{날짜}.xlsx` + csv. **7시트** : 1.요약(14칸, 3일↑ 분홍, 틀고정, 줄바꿈) / 2.할 일 / 3.수량·규격 변경 / 4.대외 언급 금지 / 5.리스크 / 6.타부서 전달 / 7.전체 내용(절마다 한 줄, 하나도 안 빠짐). `finish_xlsx` 로 값까지.
- **③ 이메일** : `t35.send(..., files=)` 로 첨부 지원(MIMEBase+한글 파일명 헤더), `_morning_or` 가 요약 엑셀을 `ATTACH` 에 담고 제목 꼬리 `SUBJ` 를 만든다 → 「[KM] 아침 한 장 2026-09-17 · 미확인 회의 3건(3일↑ 2)」.
- **④ 클로드용 md** : 「## 미확인 회의록」 절에 회의별 안건·결정·조치·할 일·변경·대외금지를 **줄로** 넣는다. 새 창은 원문 docx 를 다시 읽지 않고 이 절만 읽어 요약한다(토큰 최소).
- **버그 수정** : `t42.section()` 이 머리글 줄의 남은 글자를 내용으로 잡아 「수량·규격 변경 : 변경」 「★대외금지 : 언급 금지 사항」 같은 헛줄이 생겼다 → 머리글 줄 끝(개행) 다음부터 읽는다. 자가 시험에 그 항목 추가.
- 39 자가 시험 **40/40** (요약 엑셀 7시트·csv·md 상세·머리글 누수 5개 추가).

## 남은 일
- 결정대기 **★급함 5건** : 현장대장 준공일 / 수금대장 금액 / 견적서 원틀 / 사진대지 육안확인 / 설치사진 위치
- 겹치는 도구 4개(16·13·14·9) 정리 — `시작.bat` 이 이미 하는 것
- 인수인계 문서 남은 4건 : 아침5시 메일 / km-19 스킬 개정 / 시작.bat 한 줄 / 양양쏠비치 중복
- 원틀 3종(자재사양서·시방서·제안서) 미수령


## 2026-09-17 v39 — 「비서답게」 : 할 일을 나열하지 말고 보낼 문안까지 낸다

프로님 말씀 그대로 : *"제안서를 만들어야 되면 어떻게 만들어야 되겠다고 방법을 제시해 주던가? 견적서를 요청하면 도면이 있으면 네가 견적서를 만들고, 도면이 없으면 도면을 넣어 달라고 보고하고 이메일을 보내달라고. 그러면 어떤 내용으로 보내달라 그러는지 네가 그 이메일을 작성을 해주고 비서답게 일을 할 수 있는 수준으로 니가 좀 높아졌으면 좋겠어."*

### 새 창이 지켜야 할 규칙 (km-34 19번 · km-31 13번 과 같은 내용 — 이중 저장)
「○○ 하셔야 합니다」 로 끝내지 않는다. **프로님이 복사·붙여넣기만 하면 되는 문안**까지 낸다.

| 요청 | 도면·자료 있음 | 도면·자료 없음 |
|---|---|---|
| 견적서 | 수량을 뽑아 실행·견적서를 만든다 (단가는 단가장, 없으면 `[ ]`) | 「도면이 없다」고 보고하고 **도면 요청 메일 본문을 써서 준다** |
| 제안서 | 필요한 것·장수·구성(7장 안)을 먼저 내밀고, 주시면 만든다 | 같음 + 자료 요청 메일까지 |
| 작업의뢰서 | 부서별 본문을 쓰고 「프로님이 채우는 칸」을 표시 | 같음 |

- 도면 요청 메일 필수 항목 : 현장명 · 객실수 · 도면 종류(객실 평면 / 천장도 / 전기 계통도 / 객실관리 계통도) · **캐드+PDF 둘 다** · 성급·준공일·베란다·거실·책상 유무 · 도면이 아직 없으면 룸타입 1장이라도 · **받으면 며칠 안에 무엇을 드리는지**
- **금액·수량·요율·기한은 `[   ]` 빈칸.** 도구도 클로드도 정하지 않는다
- **대외 발송문에 내부 사정을 쓰지 않는다** : 내부 비판·손해배상·타사 단가·「대외 언급 금지」 항목 전부 제외. 당사 귀책은 당사가 진다는 어투로 닫는다
- 급한 순으로 번호를 붙인다 (오늘 낮까지 / 오늘 / 이번 주)
- 대화창 산출 세트 : `YYMMDD_오늘_실행묶음.html` + `YYMMDD_보낼메일_모음.txt`(cp949)

### 도구로 굳힌 것 — 45 요청 분기 (`t45_askgate.py`, 토큰 0)
- 현장마다 도면 유무를 보고 **견적가능 / 도면필요** 로 갈라 준다
- 도면필요 → `_도구결과\요청분기\{날짜}\보낼메일_도면요청_{현장}.txt`(cp949) 를 **미리 써 둔다**. 회의록에 제안·자료 요청이 있으면 `보낼메일_제안서자료_{현장}.txt` 도. 전부 모은 `보낼메일_모음.txt`
- 담당자·객실수·성급·준공일은 **43 확정 대장 → 현장대장** 순으로 가져오고, 없으면 「미확인」 이라고 쓴다 (짐작하지 않는다)
- **36번(오늘 한 방에) 안에서 42 앞에 저절로 돈다.** 42 아침 한 장 **1층**에 빨간 줄 「(현장) 도면이 없어 견적을 못 만듭니다 → 메일 본문 준비됨」 + 파일 링크
- 41 총괄 점검에 **G 견적·제안 요청** 구역이 생겼다 (도면 없어 막힌 현장 수 / 메일 준비 여부 / 담당자·객실수 없는 현장 → 43번 안내)
- 39 자가 시험 **46/46 통과** (45 검사 6항목 : 메일 생성·필수 항목·빈칸 유지·판 생성·1층 표시·도면 있는 현장엔 안 만듦). 시험자료에 `현장대장_시험.csv` 추가
- 메뉴 42종. `사용법.txt` 에 45번 절 추가

### 9/16 통화 11건은 이미 현장별로 정리해 드렸다 (대화창 전달)
`260916_현장별_회의정리.html` + `.xlsx`(9시트) — 클리프비치 3건 / 앵커호텔 2건 / 연합기숙사 3건 / 조선호텔 1건 / 농협 변산수련원 1건 / 신세계 ILC 1건.
`260917_오늘_실행묶음.html` + `260917_보낼메일_모음.txt` — 보낼 메일 9통 · 작업의뢰서 5건 · 제안서 2건 만드는 법 · 견적서 분기표.
**PLAUD 파일명 오류 하나** : 「더힐이앤씨 정병권선임 청명중학교」 녹음의 실제 화자는 **이요한 선임**, 현장은 **킨텍스 앵커호텔**. 원인 미확인.

### 아직 프로님 PC 에서 확인 못 한 것
98 → 36 → 41(G 구역) → 42(1층 도면요청 줄) → 45 → 43 → 44 / 바로가기 / 업무판 csv 실제 머리글 / `_현장비서` 실제 폴더 이름 / 35번 메일 설정


## 2026-09-17 v40 — 체계를 바꿨다 : 「만드세요」 가 아니라 「제가 만들까요?」 (최고 우선 스킬 km-operating-rules v44 §0.7)

프로님 말씀 그대로 : *"매일 아침 이걸로 보내. 서류 만들 게 있으면 나한테 만들라고 명령하라. 그러지 말고 네가 만들까요 하고 물어봐. 이건 최고 우선 스킬에다 넣어. 내가 생각 못 해 가지고 얘기를 못 할 수도 있는데 네가 챙겨 가지고 이렇게 할까요 하고 나한테 물어봐야지. 나보고 이거 만들어 하고 내가 생각해 가지고 얘기하는 거는 내가 까먹을 수도 있는데. 그럼 일 처리가 안 되잖아. 체계를 바꾸라고."*

### 새 창이 지켜야 할 규칙 (km-operating-rules v44 §0.7 · km-20 v2 §6 · km-34 20번 — 이중 저장)
1. 「○○ 작성 → 프로님」 「○○ 하셔야 합니다」 줄은 전부 위반. **「제가 ○○ 만들까요?」** 로 쓴다.
2. 프로님이 말씀 안 하신 서류도 회의록·도면·수금에서 보이면 **먼저** 「이렇게 할까요?」 로 내민다.
3. 답변 끝 객관식 **첫 문항 = 「제가 ○○ 만들까요?」**. 예=엔터 한 번이면 만든다. 여러 개면 묶어서.
4. 금액·수량·요율·업체 양보·현장 사실은 프로님만 아신다 → `[   ]` 비운 초안을 만들고 「이 칸만 채워 주십시오」.
5. 새 창 첫 답변 : `_아침한장.md` 「## 제가 만들까요?」 절을 읽고 그것부터 묻는다.

### 도구로 굳힌 것 — 46 앞으로 해야 될 것 (`t46_plan.py`, `facts.plan_*`, 토큰 0)
- **판단은 클로드, 배달은 파이썬.** 클로드가 회의록을 읽고 받은답 `앞으로,현장,때,등급(확정/제안),할일,누가,왜,만들기` → `_도구결과\_대장\앞으로할것.csv` (중복 안 쌓임, 완료는 상태=완료)
- 42 아침 한 장 : 1층(오늘·지남·3일 내, 보라 = 제가 ○○ 만들까요?) · 2층 현장 카드 표 · 타일 · md 「## 제가 만들까요?」「## 앞으로 해야 될 것」
- 35 아침 메일 : 제목 「오늘 할 것 n건 · 제가 만들까요 n」 + 엑셀 3시트 첨부 → **매일 아침 이걸로 간다**
- 46번 : 목록 → 번호로 완료. 받은답 `앞으로완료,현장,할일 일부` 도 됨
- 39 자가 시험 v40 검사 9항목 추가 (아침 한 장·md·엑셀 3시트·받은답 앞으로/앞으로완료·완료 줄 안 뜸)

### 오늘 넣을 것 (프로님 PC)
`앞으로_260917_받은답.csv` (41줄, 제가 만들까요 23건) 를 `3_공통사용\단가장\받은답\` 에 넣고 36번(또는 30번) → 대장에 들어가고 내일 아침 메일부터 나온다.
9/17 「현장별 앞으로 해야 될 것」 HTML·엑셀·메일은 이미 전달했다(메일 1통, 프로님 주소로만).

### 아직 프로님 PC 에서 확인 못 한 것
98 → 36 → 42(1층 보라 글씨) → 46 → 35 제목 / 스킬 7개 저장 (km-operating-rules v44 · km-20 v2 가 새로 추가됨)


## 2026-09-17 밤 — 「0_시작전_반드시_읽기_v1.md」 (km-00-first-read v1) + 메일 주소
- 프로님 : *"반성할 것과 내가 하라고 했는데 잘 잊는 것을 핵심만 번호로 나열해. 그것이 기준이 되어 채팅을 시작할 때 무조건 읽고 시작한다."* → 30줄 목록을 스킬 `km-00-first-read` 와 이 폴더 `0_시작전_반드시_읽기_v1.md` 에 이중 저장. **새 창은 이것부터 읽는다.** 전달보고 ④ 는 이 번호로 적는다.
- **아침 메일 받는 주소 = bsy@micronic.co.kr** (프로님 지정). 35번 설정 기본값을 이 값으로 바꿨다. 9/17 「앞으로 해야 될 것」 메일을 이 주소로 다시 보냈다.
- 「23건 전부 만들까요?」 는 프로님이 시간이 없어 나중에 보시기로. 답이 오면 그때 만든다.

- **31번 추가 (km-00 v2)** : 프로님이 잊고 저장 안 하신 스킬·파일을 새 창이 먼저 알린다. `인수인계함\스킬대조.py` 가 저장대기 vs 설치본을 비교한다. **2026-09-17 밤 기준 8개 전부 미저장** (설치본 km-operating-rules v43 / km-20 v1 / km-19 v2 / km-32 v1 / km-33 v1 / km-34 v2 / km-31 옛 판 / km-00 없음). 새 창 첫 답변 맨 위에 이 표부터.


## 2026-09-18 v41 — 견적 보낸 곳을 찾아간다 + PLAUD 직접 읽기의 값

### 프로님 지시
> "시작할 때 읽을 것에, 견적을 보낸 곳을 찾아가 봐야 된다고 방문해야 할 것을 메일에 적게 해."
> "(PLAUD 를 직접 읽는 것은) 사용량이 얼마나 드는가? 사용량을 떠나서 더 좋다면 인정할게."

### 47 견적 보낸 곳 · 찾아갈 곳 (`t47_visit.py`, `facts.quote_*`)
- 대장 `_도구결과\_대장\견적발송.csv`. **보낸 뒤 3일** 안 가보셨거나 **다녀온 뒤 14일** 지나면 42 아침 한 장 1층·타일·md 「## 찾아갈 곳」·35 메일 제목(「찾아갈 곳 n곳」)+엑셀에 **매일**
- 넣는 길 : 받은답 `견적발송,현장,받는곳,담당자,금액,메모` / 47번 / csv. 다녀오시면 47번 번호 또는 `방문,현장,메모`
- **클로드가 견적·제안서를 만들어 드리면 `견적발송` 줄을 같이 낸다** (프로님이 따로 적지 않으시게)
- km-00 v3 **32번**, km-31 **15번** 에 같은 내용 이중 저장

### PLAUD 직접 읽기 — 실측값 (km-00 v3 33번)
- 2026-09-18 주간 정리(9/14~17) : 녹취 목록 45건 + **AI 요약 15건** + HTML/엑셀/메일 = **약 15만 토큰**
- 매일 하면 주 70만이 넘는다 → **평소는 PC 도구(0원)**, 클로드 직접 읽기는 ① 「이번 주 정리해」 ② 특정 통화 지목 ③ PC 도구가 못 잡은 회의 뿐
- 읽기 전에 **몇 건·대략 얼마인지 먼저 말씀드린다.** 한 번 읽은 회의는 txt 로 남겨 다음 창이 다시 읽지 않게
- 그럼에도 직접 읽기가 나은 점 : 프로님 손이 0 (PC 저장·시작.bat 불필요), 파일명이 틀려도 내용으로 현장을 잡음, 1분 미만 통화까지 목록으로 확인

### 이번 주 산출물 (대화창·메일 전달 완료)
`260918_이번주_현장정리_0914-0917.html` + `.xlsx`(10시트) + 메일 1통 → 현장 8곳 · 앞으로 48건 · 제가 만들까요 29건
새로 나온 현장 : **머큐리 앰버서더 호텔(명동)** 목업 2세대, 2000 시리즈 지정, 10월 공사(입찰)
`조선호텔_중도금청구서_v1_260918.docx` + 화면 확인용 HTML (금액칸 `[ ]`)

### 못 읽은 녹취 (프로님이 말씀하시면 읽습니다)
9/15 권진희 과장 3건·홍승조 부장 3건 / 9/17 김경찬 팀장 3건·안성규·현대네트웍 / 1분 미만 30건


## 2026-09-18 v43 — 아침 메일 하루 두 번 + 읽기 쉬운 회의록 절

### 프로님 지시 (원문)
> "내가 사람 입장에서 볼 땐 날짜가 적혀 있는 것이 더 보기 쉽다 — 날짜 넣어. PC는 동일한 내용을 앞에 반복해서 혼동된다.
> 결정을 4번 쓰고 조치를 4번 쓰지 말고 1번 쓰고 내용은 그 아래에 누락 없이 적으면 보기 편하다."
> "pc가 돌려서 내 메일로 보내는 것으로 해. 매일 파이썬이 아침 7시에 보내고, 내가 7시 전에 어제 PLAUD 회의록을 못 넣었으면
> 7시 것은 PLAUD 빼고 할 일은 해. 그 후 9시에 출근해서 회의록 넣고 시작을 누르면 그때 어제 회의록을 포함해서 메일을 보내."

### 1) `_아침한장.md` 회의록 절 형식 (km-31 17번 · km-34 21번)
```
- **2026-09-15 · 조선호텔 · 진현창 파트장**  [3일 지남]
  - 안건 : 기구물 입고, 인테리어 공정, 현장 관리, 1차 기성
  - 결정 4건
    · 09-15  없음 — 기구물 입고 일정 확인 후 재협의 예정
    …
  - 리스크 3건
    · 09-15  1차 기성 인식 차이(끊었다 vs 못 끊었다) → 내부 청구 내역 확인 후 회신
```
라벨 한 번 + 건수 + 날짜 붙은 줄. **누락 금지** — 줄 수를 줄이려고 내용을 빼지 않는다.

### 2) 아침 메일 하루 두 번 (km-31 16번)
| 편 | 언제 | 무엇이 들어가나 | 제목 |
|---|---|---|---|
| ① 아침 | **07:00** 작업 스케줄러 | 회의록이 없어도 오늘 할 것·찾아갈 곳·수금·도면·점검 전부 | 「… · 어제 회의록 아직 안 들어옴」 |
| ② 회의록 | 회의록 넣고 **36번(시작)** | 7시 이후 새로 들어온 회의가 있을 때만 | 「… · 어제 회의록 포함(새 회의 n건)」 |

- 기록 : `_도구결과\_대장\메일발송.csv` (날짜·구분·회의건수·보낸시각·제목·결과) — 같은 날 같은 편은 두 번 안 간다
- 7시 전에 이미 넣으셨으면 7시 메일에 다 들어가므로 재발송 없음
- 등록 : 35번 → 3번 (`KM_아침메일_0700`). PC 가 꺼져 있으면 그날 7시 편은 안 감
- 코드 : `t35.auto()` / `t35.after_meeting()` / `t36` 끝에서 호출

### 3) 확인한 것
- 39 자가 시험 **68/68 통과** (md 형식 3항목 + 메일 5항목 추가)
- 메일 실물 샘플 2통을 만들어 대화창에 전달 (07시 편 / 09시 편)
- 스킬 8개는 프로님이 **저장 완료** (이 창에서 확인됨 — km-00 v3 · rules v44 · km-20 v2 · km-31 · km-32 v2 · km-33 v2 · km-34 v7 · km-19 v5)


## 2026-09-18 v44 — 판이 한 방향이 아니다 (A안 회신 + C안 대화)

### 프로님 지시
> "매일 메일로 보내는 것은 너무 일방적이야. 니가 보낸 것이 맞을 수도 틀릴 수도 있는데, 내가 확인을 해줘야 니가 반영을 할 수 있어.
> 그걸 내가 바로잡아서 너와 소통을 해야 하는데 방법을 만들어." → **A안(메일 회신) + C안(대화)** 채택, 업그레이드 지시

### 무엇을 만들었나 (48번 `t48_reply.py`)
1. **고정 번호** — 앞으로할것 대장에 `코드` 열(KM-001…). **끝날 때까지 안 바뀐다** (어제 메일 보고 오늘 답해도 맞음)
2. **회신 단추** — 42 아침 한 장 1층 줄마다 mailto 단추 4개 [완료][맞아][아니야][만들어줘]. 누르면 회신이 **미리 써진 메일**이 열린다(타이핑 없음)
3. **받는 길 3가지** — ① 메일함 IMAP ② `받는함\회신.txt` ③ 대화(받은답 `회신,KM-003,아니야 …`)
4. **한 줄 문법** — `완료 / 확인·맞아 / 아니야 (고칠 내용) / 만들어줘 / 취소 / 미뤄 (날짜)`
   - 짧게 답하시면 원래 할 일을 지우지 않고 **「원래 → 고친 것」** 으로 남긴다(맥락 보존)
   - 그 밖의 말은 **「못 알아들음」** 으로 남겨 다음 메일에 되묻는다 — 버리지 않는다
5. **소통 이력** — `_대장\소통이력.csv` → `_아침한장.md` 「## 프로님이 고치신 것(최근 7일)」·「## 제가 못 알아들은 답」 → **새 창도 읽는다**
6. **36번이 판을 만들기 전에 48번을 먼저 돌린다**

### 확인한 것
- 39 자가 시험 **78/78 통과** (48번 검사 10항목 추가)
- 실제로 돌려 본 회신 5줄 : `KM-003 아니야 1개 층 선납으로` → 수정+확정 대장 / `1 완료` / `5 만들어줘` / `6 미뤄 다음주` / `2 음 글쎄` → **못 알아들음으로 되묻기**

### ★ 사고 기록 (같은 실수 금지)
`common.py` 를 **빈 파일로 날렸다**. 원인 : `io.open(p,'w').write(io.open(p).read())` — 쓰기로 열면서 파일이 먼저 비워졌다.
→ **반드시 먼저 읽어서 변수에 담고, 그 다음에 쓴다.** git 에서 즉시 복구했고 자가 시험으로 확인(78/78). km-00 13번(기억 파일 손상)의 재발이다.


## 2026-09-18 v45 — 섞어 보내지 않는다 (헛짓 방지)

### 프로님 지시
> "아침에 메일을 꼭 한 메일에 몰아서 안 해도 돼. 너가 무리가 안 간다면 한 번에 해도 되고.
> 따로따로 나누어서 **니가 나중에 헛짓을 안 하게** 하는 방향으로 해야 돼."

### 진단 — 헛짓은 어디서 나는가
확인 안 된 **제안**과 이미 정해진 **확정**이 한 덩어리로 섞이면, ① 내가 제안을 확정으로 착각해 서류를 만들고 ② 프로님이 일부만 답하셨을 때 나머지가 어떻게 됐는지 알 수 없다. **그래서 성격으로 가른다.**

### 무엇을 만들었나 (`t35.send_parts()`)
| 편 | 담기는 것 | 회신 |
|---|---|---|
| **① 답해 주십시오** | 등급=제안만 · 고정 번호 + mailto 단추 4개 + 못 알아들은 답 되묻기 | **여기에만** |
| **② 오늘 할 것** | 등급=확정만 · 단추 없음 | 필요 없음 |
| **③ 어제 회의록** | 새 회의가 들어온 날만 (아침 한 장 전체) | — |

- 회신이 ①번에만 오므로 48번이 어느 줄에 대한 답인지 헷갈리지 않는다
- 한 통으로 받으려면 `설정.ini [메일] 나눠보내기 = 아니오` (35번 → 1 설정에서도 물어본다)

### ★ 헛짓 방지 규칙 (km-00 35번 · km-31 19번 · km-34 23번)
**확인 전 제안으로는 서류도 의뢰서도 만들지 않는다.** 3일 넘게 답이 없으면
- 버리지 않고 · 멋대로 진행하지도 않고
- `t46.waiting()` 이 **「묵은 제안」** 으로 올려 ①번 메일 맨 위 + 42 1층 + `_아침한장.md` 「## 답을 못 받아 진행하지 않은 것」 에 띄운다

### 확인한 것
- 39 자가 시험 **83/83 통과** (v45 검사 5항목 추가)
- 실제 샘플 : ①번 3건(묵은 제안 4일째 1건 포함) / ②번 8건 — 대화창 전달


## 2026-09-18 v46 — 회신 단추 5개(진행중) + 「새 지시로 옛 지시를 덮지 않는다」

### 프로님 지시 (원문)
> "진행중 을 넣어줘."
> "완료 표시 하면 어떻게 너는 기록해서 나중에 내가 확인 할 수 있나?
>  맞아 표시는 그냥 니가 확인 한거야? / 아니야 표시는 하면 어떻게 기록해서 나중에 확인 할 수 있나?
>  만들어줘 표시는 니가 만들 때 어떻게 만들지 나하고 상의 하는 거야?"
> "중요한건 양방향 한다고 이렇게 되었는데, 이것은 일부이니 메일로 보내는 것이고,
>  다른 것도 메일로 보내기로 한것은 메일로 보내야되는거야.
>  한가지 시켯다고 그전거 보내지 말라고 않했는데, 중지 하지 말고.
>  중복이나 업그레이드면 나에게 말하고 업그레이드로 적용해. - 스킬로 저장해."

### 1) 회신 단추 5개 — 각 단추가 남기는 자리 (km-00 37번 · km-31 20번)

| 단추 | 대장에 벌어지는 일 | 프로님이 보시는 곳 |
|---|---|---|
| **진행중** | `앞으로할것.csv` 상태=진행중. 줄을 닫지 않는다 | `_아침한장.md` 「## 진행중」 |
| **완료** | 상태=완료 + 완료일. **줄을 지우지 않는다** | `앞으로할것.csv` 완료 줄 · 46번 |
| **맞아** | 제안 → 확정. **이미 확정이면 대장이 안 바뀐다** | `소통이력.csv` (그 사실을 회신 답에 그대로 적는다) |
| **아니야** | 할 일이 「원래 → 고치신 것」 으로. **확정 대장에도 한 줄** | 확정사항 대장 · 「## 프로님이 고치신 것」(7일) |
| **만들어줘** | **바로 안 만든다.** 상태=만들기대기 | 「## 제가 이렇게 만들겠습니다」 → 「맞아」 받고 만든다 |

다섯 개 전부 `소통이력.csv` 6칸(날짜·경로·번호·프로님 말씀 그대로·내가 이해한 것·내가 한 일)에 통째로 남는다.
**이미 완료·취소된 줄은 진행중·만들어줘로 다시 열지 않는다.**

### 2) 고친 코드
- `t48_reply.py` : `WIP` 낱말묶음 신설(`OKAY` 의 '진행' 보다 **먼저** 걸러야 해서 `ASK` 다음·`DONE` 앞) / `만들기` → 상태=만들기대기 + 「먼저 여쭙는다」 / `확인`(맞아) 이 이미 확정이면 그 사실을 답으로 / 완료·취소 줄 보호 / **`미룸` 값 고침** — 「다음주로 미뤄」 가 `로 미뤄` 로 잘리던 것 → 시키는 말만 빼고 때는 남긴다, 아무것도 없으면 `미정`
- `t42_morning.py` : md 절 2개 추가 — 「## 진행중」 · 「## 제가 이렇게 만들겠습니다」. **기존 절은 하나도 빼지 않았다**
- `common.py` : VERSION v45 → v46

### 3) 「새 지시로 옛 지시를 덮지 않는다」 (km-00 36번 · km-34)
- 새 지시는 **더하기**로 받는다. 그만하라는 말은 프로님이 그만하라고 하실 때만 나온다.
- 겹치면 없애지 말고 **「겹칩니다, 업그레이드로 합치겠습니다」** 라고 먼저 말씀드린 뒤 **합친다.** 프로님께 골라 받으라고 떠넘기지 않는다.
- 샘플·새 안을 보여 드리는 날에도 **기존에 보내기로 한 것은 그대로 나간다.**
- 아침 한 장 절 목록은 **줄이는 쪽으로 못 바꾼다** (묶기는 O, 빼기는 C등급 사전 확인).

### 4) 아직 안 한 것
- 새 안(메일 1통 + 현장별 한 장)은 **샘플만** 있고 `t42`·`t35` 에 아직 안 넣었다. 프로님 「넣어」 를 기다린다.
- 넣을 때 **기존 3통 방식을 지우지 않고** `설정.ini [메일] 보내는방식 = 한장 / 세통 / 한통` 으로 골라 쓰게 한다 (36번).
