# 음식점 홈페이지 — 설치·운영 안내

`restaurant.html` 한 장이 홈페이지 전부다. 서버 없이 GitHub Pages로 공개한다.
현재 내용은 **가상의 가게「해온식당」견본**이며, 아래 순서대로 실제 값으로 바꾸면 된다.

---

## 1. 사장님께 받아야 할 것 (그대로 복사해서 보내면 됨)

```
홈페이지 만들려고 하는데 아래만 알려주세요.

1) 상호 (간판에 적힌 그대로)
2) 한 줄 소개 — 손님한테 뭘 제일 내세우고 싶으신지
3) 주소 (도로명) / 전화번호
4) 영업시간 — 요일별로, 브레이크타임·정기휴무 포함
5) 주차 — 몇 대 가능한지, 무료인지, 만차면 어디로
6) 메뉴 전부 — 이름 / 가격 / 짧은 설명, 대표메뉴 3개 표시
7) 사진 — 대표메뉴 3장 + 가게 내부 1장 (휴대폰으로 찍으신 걸로 충분합니다.
   음식은 창가 자연광에서 위에서 찍은 게 제일 잘 나옵니다)
8) 이미 쓰고 계신 것 있으면 주소 — 네이버 플레이스, 인스타, 배달의민족,
   네이버 예약, 카카오톡 채널
9) 사업자등록번호 / 대표자명 (홈페이지 하단에 넣습니다)
```

**사진이 없으면 홈페이지 품질의 8할이 날아간다.** 8번은 나중에 받아도 되지만 7번은 꼭 받는다.

---

## 2. 값 바꾸기

`restaurant.html` 안에서 두 군데만 고치면 된다.

| 위치 | 무엇 |
|---|---|
| `<script>` 안의 `const SHOP = {...}` | 전화·주소·요일별 영업시간·브레이크타임 |
| 본문 HTML | 상호, 한 줄 소개, 대표메뉴 3개, 주차 안내, 각종 링크, 사업자 정보 |

전화번호는 `tel:0212345678` 형태(하이픈 없이)로 **4곳**에 들어있다 — 히어로·예약카드·하단바·푸터. 한꺼번에 바꾼다.

사진은 `img/` 폴더를 만들어 넣고, 회색 자리(`<div class="ph">…</div>`)를
`<img src="img/kimchi.jpg" alt="돼지김치찌개">`로 교체한다.
**사진은 올리기 전에 가로 1200px 정도로 줄인다.** 원본 그대로 올리면 모바일에서 느려진다.

---

## 3. 메뉴를 사장님이 직접 고치게 하기 (구글시트 연동)

1. 구글시트 새 문서를 만들고 첫 줄에 머리글을 넣는다:

   | 분류 | 메뉴명 | 가격 | 설명 | 품절 |
   |---|---|---|---|---|
   | 식사 | 돼지김치찌개 | 9000 | 3년 묵은지 + 목살 | |
   | 안주 | 모둠전 | 18000 | | Y |

   - 가격은 숫자만 (`9000`). 콤마·"원"은 페이지가 알아서 붙인다.
   - 품절 칸에 `Y`를 적으면 그 메뉴에 취소선과 「품절」 표가 붙는다.
   - 분류 이름이 그대로 메뉴판 탭이 된다.

2. 시트에서 **파일 → 공유 → 웹에 게시 → 형식 `쉼표로 구분된 값(.csv)` → 게시**
3. 나온 주소(`https://docs.google.com/spreadsheets/d/e/2PACX-.../pub?output=csv`)를
   `restaurant.html`의 `const MENU_CSV_URL = '';` 안에 붙여넣는다.

시트를 고치면 홈페이지에 반영된다(구글 쪽 갱신 지연이 있어 즉시가 아니라 몇 분 걸린다).
**시트를 못 읽어도 화면은 비지 않는다** — `FALLBACK_MENU`의 기본 메뉴가 대신 나온다.

> ⚠ 이 연동 경로는 아직 실제 구글시트로 시험하지 못했다. 시트를 만든 뒤 한 번 확인해야 확정이다.

---

## 4. 공개하기 (GitHub Pages)

저장소 **Settings → Pages → Source: `main` 브랜치 / 루트** 로 지정하면
`https://<계정>.github.io/mic-pm/restaurant.html` 로 열린다. 무료다.

---

## 5. 도메인 — 반드시 **사장님(가게 주인) 명의**로 등록

가게 이름으로 된 주소(`가게이름.kr`)를 쓰려면 도메인을 산다. 연 1.4만원대(.kr 기준, 첫해 할인 있음).

**명의가 핵심이다.** 대행해 준 사람 명의로 등록해 두면, 나중에 연락이 끊기거나 갱신을 놓쳤을 때
가게가 자기 주소를 잃는다. 실제로 자주 벌어지는 사고다.

- 등록·결제 계정: **사장님 것**
- 결제카드: **가게 것** (자동갱신 켜둘 것)
- 우리는 연결 설정만 도와주고 계정은 넘긴다

연결 방법: 도메인 관리 화면에서 `CNAME`을 `<계정>.github.io`로 지정하고,
저장소 Settings → Pages → Custom domain에 도메인을 적는다. HTTPS는 GitHub이 자동으로 붙여준다.

---

## 6. 만들고 나서 해야 진짜 손님이 오는 것

홈페이지만 만들면 아무도 못 찾는다. 순서는 이쪽이 먼저다.

1. **네이버 스마트플레이스 등록** — 한국에서 식당 검색의 대부분이 여기서 일어난다. 무료.
2. 스마트플레이스·인스타 프로필에 홈페이지 주소를 건다.
3. 구글 비즈니스 프로필 등록 (외국인 손님·구글맵용).
4. 메뉴가 바뀌면 시트만 고친다.
