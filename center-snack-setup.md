# 센터간식 홈페이지 — 운영 안내

`center-snack.html` 한 장이 홈페이지 전부다. 서버도 월 요금도 없다.

## 지금 상태

| | |
|---|---|
| 실제 값이 들어간 것 | 상호 · 주소 · 전화 · 메뉴 약 30종 가격 · 세트 A/B/C 금액 |
| 아직 못 정한 것 | 영업시간 / 건물명 / 단체주문 마감·배달시간·최소인원 확정 / 음식 사진 |

세트 금액은 짐작이 아니라 **매장 단품 정가의 합**이다.

| 세트 | 구성 | 계산 |
|---|---|---|
| A 가볍게 | 센터김밥 5 · 오뎅 5 · 커피 5 | 17,500 + 5,000 + 7,500 = **30,000** |
| B 든든하게 | 센터김밥 5 · 클럽샌드위치 5 · 커피 5 | 17,500 + 17,500 + 7,500 = **42,500** |
| C 야근 | 제육덮밥 5 · 라볶이 2 · 커피 5 | 27,500 + 10,000 + 7,500 = **45,000** |

> ⚠ A의 원래 구성은 「꼬마김밥 15줄」인데 낱개 단가가 없어 센터김밥으로 바꿨다.
> 꼬마김밥 1줄 가격을 알려주면 원래 구성으로 되돌린다.

## 주문서가 하는 일

구글폼도 네이버폼도 쓰지 않는다. 페이지 안에서 끝난다.

1. 손님이 **인원 ±** 를 누른다 → 추천 구성이 인원수만큼 자동으로 늘어난다
2. **A / B / C** 를 누르면 품목이 채워지고, 품목별 **± 로 직접 조정**할 수 있다
3. 합계와 1인당 금액이 즉시 계산된다
4. **「내용 채워서 문자 보내기」** → 아래 내용이 채워진 문자가 열린다. 손님은 보내기만 누른다

```
[센터간식 단체주문]
· 인원 : 8명
· 받는 시간 : 오전 11시
· 메뉴 : 제육덮밥 8, 라볶이 3, 커피 8
· 합계 : 71,000원
· 받는 날짜 :
· 회사 / 호실 :
· 연락처 :
```

문자가 안 되는 환경을 위해 **「주문 내용 복사」** 버튼도 같이 뒀다(카카오톡에 붙여넣기용).

## 값 고치는 곳

`center-snack.html` 맨 아래 `<script>` 안 두 군데만 고치면 된다.

```js
var CATALOG = [ {id:'deopbap', name:'제육덮밥', price:5500}, ... ];   // 품목과 가격
var RECIPE  = { A:{...}, B:{...}, C:{...} };                          // 추천 구성
```

전화번호는 `TEL` / `TELRAW` 두 변수와 본문 링크에 들어 있다.

## 공개하기

저장소 **Settings → Pages → Source: `main` / 루트** → `https://<계정>.github.io/mic-pm/center-snack.html`. 0원.

도메인을 붙이면 `센터간식.kr` 같은 주소가 된다(연 1.4만원대). **반드시 사모님 명의로 등록**한다 — 대행자 명의로 두면 나중에 못 가져온다.

## 아직 안 넣은 것

- **음식 사진** — 데스크톱 히어로 오른쪽이 비어 있다. 7/31 단체도시락 사진 16장을 받으면 채운다.
- **지도** — 카카오맵 삽입 자리만 있다. 좌표를 받으면 넣는다.
- **QR** — 폼이 아니라 이 페이지 주소로 QR을 만들어 배너·엘리베이터 A4에 넣으면 된다.

## 톤 규칙 (km-center-snack §4)

- 노랑·빨강·큰 글씨 분식 톤 금지 → 먹빛 `#0B0F14` · 금색 `#C6A16A` · 크림 `#F4F1EA` 세 색만
- "올라오세요·2층입니다" 애원 문구 금지 → 위치는 맨 아래 `↑ 2F` 한 번만
- 이모지·클립아트 금지 → 아이콘은 전부 선으로 그린 벡터, 심볼은 도시락 칸 기하 마크
